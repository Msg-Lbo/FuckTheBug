{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口和设置窗口允许监听配置事件并执行签名更新",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "updater:default",
    "process:allow-restart"
  ]
}
